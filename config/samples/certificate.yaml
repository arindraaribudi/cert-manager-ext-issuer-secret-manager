apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: aws-sample-cert
  namespace: cert-manager
spec:
  secretName: aws-sample-cert-tls
  issuerRef:
    name: aws-sample
    kind: AWSSecretManagerIssuer
    group: secret-manager.cert-manager.io
  dnsNames:
    - example.local
